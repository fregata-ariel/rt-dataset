ARG RUNTIME_IMAGE
FROM ${RUNTIME_IMAGE}

# CI専用のテスト/Lintツール (本番用のruntimeイメージには含めない)
# ruff はルールがバージョンで変わるため uv.lock と同じバージョンに固定する
RUN uv pip install --system --no-cache "pytest>=8.0.0" "ruff==0.16.4"
