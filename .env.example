# docker compose の設定 (任意)。cp .env.example .env で使う。
# ベースにする nvidia/cuda イメージを差し替える場合に指定する (既定: 12.8.1-base-ubuntu24.04)。
# Dr.Jit/Mitsuba はホストのドライバを使うため、通常は変更不要。
# CUDA_BASE_IMAGE=nvidia/cuda:12.8.1-base-ubuntu24.04
